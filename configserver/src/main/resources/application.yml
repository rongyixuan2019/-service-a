spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rongyixuan2019/-service-a.git
          search-paths:
          - configfiles
      label: dev
server:
  port: 8888
